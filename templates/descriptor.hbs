class {{pluginPrefix}}GrailsPlugin {
    def groupId = "{{groupId}}"
    def version = "{{version}}"
    def grailsVersion = "2.4 > *"

    def title = "{{title}}"
    def author = "{{author}}"
    def authorEmail = "{{email}}"
    def description = "{{description}}"

    def documentation = "{{repositoryUrl}}"

    def organization = [name: "OpusCapita", url: "http://www.opuscapita.com/"]
    def developers = [[name: "{{author}}", email: "{{email}}"]]
}
